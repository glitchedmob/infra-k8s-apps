apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: argo-workflows-sso-login-redirect
  namespace: argo-workflows
spec:
  redirectRegex:
    regex: '^https://workflows\.levizitting\.com/login\?redirect=(.+)$'
    replacement: 'https://workflows.levizitting.com/oauth2/redirect?redirect=${1}'
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: argo-workflows-sso-login-default
  namespace: argo-workflows
spec:
  redirectRegex:
    regex: '^https://workflows\.levizitting\.com/login/?$'
    replacement: 'https://workflows.levizitting.com/oauth2/redirect?redirect=/workflows'
    permanent: false
