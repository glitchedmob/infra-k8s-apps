apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: levizitting-ca-bundle
spec:
  sources:
    - secret:
        name: "levizitting-root-ca"
        key: "ca.crt"
  target:
    configMap:
      key: "ca.crt"
