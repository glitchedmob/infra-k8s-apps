apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: levizitting-root-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: levizitting.com Root CA
  subject:
    organizations:
      - levizitting.com
  dnsNames:
    - levizitting.com
  secretName: levizitting-root-ca
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
    group: cert-manager.io
