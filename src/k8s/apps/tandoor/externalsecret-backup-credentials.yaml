apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tandoor-backup-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: k8up-b2-credentials
    creationPolicy: Owner
  data:
    - secretKey: account-id
      remoteRef:
        key: /vm-workloads/lz/infra-vm-workloads/backups/b2-account-id
    - secretKey: account-key
      remoteRef:
        key: /vm-workloads/lz/infra-vm-workloads/backups/b2-account-key
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tandoor-k8up-repo-password
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: tandoor-openbao
    kind: SecretStore
  target:
    name: tandoor-k8up-repo-password
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: tandoor/backup
        property: resticPassword
